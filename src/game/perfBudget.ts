/**
 * Techos de la versión final. Los props decorativos van en InstancedMesh
 * (un draw call por tipo). Solo quedan objetos sueltos los que se tocan:
 * frutales, bayas, estelas, Lilith, conejos.
 */
export const GRASS_TUFT_BUDGET = 1200;
export const ROCK_BUDGET = 80;
export const COMPASS_HUD_INTERVAL = 0.12;
